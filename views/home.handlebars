<div class="row">
  <div class="col-md-6">
    <div class="card text-white bg-primary mb-3">
      <div class="card-header">
        <h5 class="card-title">
          Active Missions
        </h5>
      </div>
      <div class="card-body form-horizontal">
        <table class="table">
          <thead>
            <tr>
              <th scope="col">ID</th>
              <th scope="col">Organization</th>
              <th scope="col">Date</th>
              <th scope="col">Description</th>
            </tr>
          </thead>
          <tbody>
            {{#each activeMissions}}
            <tr onclick="window.location='track/{{missionID}}';" style="cursor: pointer;">
              <td>{{missionID}}</td>
              <td>{{orgName}}</td>
              <td>{{formattedLaunchDate}}</td>
              <td>{{description}}</td>
            </tr>
            {{/each}}
          </tbody>
        </table>
      </div>
    </div>
  </div>
  <div class="col-md-6">
    <div id="announcementCarousel" class="carousel slide" data-ride="carousel"
    style="width: 100%; height: 200px; padding: 50px;">
      <div class="carousel-inner text-center">
        <div class="carousel-item active">
          <div class="d-flex h-100 align-items-center justify-content-center">
            <p>Mission Control is a high altitude balloon tracking
              site.  It is still under development so please be patient.  Constructive
              feedback may be sent to jlarkin@whitworth.edu
            </p>
          </div>
        </div>
        <div class="carousel-item">
          <div class="d-flex h-100 align-items-center justify-content-center">
            <p>Placeholder for info on an upcoming launch</p>
          </div>
        </div>
        <div class="carousel-item">
          <div class="d-flex h-100 align-items-center justify-content-center">
            <p>The announcement feature is still in development stage so content is static for now</p>
          </div>
        </div>
      </div>
      <a class="carousel-control-prev" href="#announcementCarousel" role="button" data-slide="prev">
        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
        <span class="sr-only">Previous</span>
      </a>
      <a class="carousel-control-next" href="#announcementCarousel" role="button" data-slide="next">
        <span class="carousel-control-next-icon" aria-hidden="true"></span>
        <span class="sr-only">Next</span>
      </a>
    </div>
  </div>
</div>
<div class="row">
  <div class="col-md-6">
    <div class="card text-white bg-primary mb-3">
      <div class="card-header">
        <h5 class="card-title">
          Planned Missions
        </h5>
      </div>
      <div class="card-body form-horizontal">
        <table class="table">
          <thead>
            <tr>
              <th scope="col">ID</th>
              <th scope="col">Organization</th>
              <th scope="col">Date</th>
              <th scope="col">Description</th>
            </tr>
          </thead>
          <tbody>
            {{#each plannedMissions}}
            <tr onclick="window.location='plannedOverview/{{missionID}}';" style="cursor: pointer;">
              <td>{{missionID}}</td>
              <td>{{orgName}}</td>
              <td>{{formattedLaunchDate}}</td>
              <td>{{description}}</td>
            </tr>
            {{/each}}
          </tbody>
        </table>
      </div>
    </div>
  </div>
  <div class="col-md-6">
    <div class="card text-white bg-primary mb-3">
      <div class="card-header">
        <h5 class="card-title">
          Completed Missions
        </h5>
      </div>
      <div class="card-body form-horizontal">
        <table class="table">
          <thead>
            <tr>
              <th scope="col">ID</th>
              <th scope="col">Organization</th>
              <th scope="col">Date</th>
              <th scope="col">Description</th>
            </tr>
          </thead>
          <tbody>
            {{#each archivedMissions}}
            <tr onclick="window.location='review/{{missionID}}';" style="cursor: pointer;">
              <td>{{missionID}}</td>
              <td>{{orgName}}</td>
              <td>{{formattedLaunchDate}}</td>
              <td>{{description}}</td>
            </tr>
            {{/each}}
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>
